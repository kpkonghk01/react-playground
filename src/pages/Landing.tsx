import React from 'react';
import FullParentWrapper from '../components/wrappers/FullParentWrapper';

const Landing = () => {
  return <FullParentWrapper></FullParentWrapper>;
};

export default Landing;
